# typos is a Source code spell checker
# https://github.com/crate-ci/typos

[files]
extend-exclude = [
  "bun.lock",
  "build",
  "dist",
  "node_modules",
  "public/open-source-notices.txt",
]

[default.extend-words]
# biome.json uses noInferrableTypes
inferrable = "inferrable"
