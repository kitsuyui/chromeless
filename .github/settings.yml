# Repository settings managed as code (https://github.com/probot/settings)
# Apply automatically by installing the "Settings" GitHub App, or apply
# manually:
#   gh repo edit kitsuyui/chromeless \
#     --description "Turn any website into a site-specific browser" \
#     --add-topic electron \
#     --add-topic site-specific-browser \
#     --add-topic chromium \
#     --add-topic typescript

repository:
  description: "Turn any website into a site-specific browser"
  topics:
    - electron
    - site-specific-browser
    - chromium
    - typescript
